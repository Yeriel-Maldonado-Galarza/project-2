typedef struct
{
  // i can turn this into 1 array. then i can make a for loop when im writing unto the file and just iterate through it to print w just one line then when im in the last one do the same but with a new line
  double time;
  double pos_x;
  double pos_y;
  double spd_x;
  double spd_y;
  double acc_x;
  double acc_y;
} robot;